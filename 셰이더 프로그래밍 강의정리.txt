[ 9.10 2주 2 ]
Vertex Shader 하나의 스테에지에서 몇 개의 입력을(vertex)를 받아서 처리하는가? -> 1개
Fragment Shader '' -> 1개 (의 픽셀)
Primitive Assembly는 세 개의 VetexShader의 출력을 필요로 한다.
Rasterization and Interpolation 에서 Fragment 의 갯수를 결정한다.
Frame Buffer는 vertex 를 구성(?)하는 픽셀들의 갯수만큼  수행된다.
(Fragment Shader -> Frame Buffer 에서 Fragment Shader 는 해당 vertex 를 구성하는 픽셀들의 갯수만큼  수행된다.)

세 정점에 각각 R G B 의 원색을 넣었는데 왜 중간 픽셀은 원색이 아닌가?
	-> 보간되었기 때문
	-> Fragment Shader는 보간된 결과를 받는다.

삼각형의 윗대가리만 날리는법?
	-> if 문과 discard 를 통해..
	-> discard가 되면, 픽셀을 버리고 그 이후의 코드는 실행되지 않는다.

[ 9.16 3주 1 ]
Shader는 Uniform 으로 시간을 받는다

[ 9.23 4주 1 ]
Shader 실시간 Reloading (매 순간 컴파일 하는 오버헤드를 없애보자)

Renderloop 안에서 DrawCall << 여기서 참조하는것은바로  Shader Program
			<< key input event 호출 -> API 호출 (또 다른 쓰레드처럼 동작)
			<< Shader Program 을 날리고, 새 프로그램을 연결시키고자.. lock() 필요?
			Drawcall 에서 Shader Program 참조를 끊고 다른 친구에게 연결하는 동작을 실습

중력 구현
	
각 파티클별로 랜덤한 생성 시간을 줘보자

CreateParticles(1000); 에서 몇 개를 넣었을 떄 부터 느려지는가? 로 컴퓨터 성능 체크 가능


[ 9.24 4주 2 ]
파티클 별로 알파값을 만들어서.. 시간에 따라서 서서히 흐려지게

Velocity 처리

lifetime

이것들로 폭죽을 만들 수 있겠다

미사일 꽁다리 불꽃처럼 만들기
	외부 힘이 가속도에 반영되도록 해보자***


[ 9.30 5주 1 ]

내일 휴강

sin , period 이용, 제트엔진 만들기
mix(A, B, value) 


[ 10.14 7주 1 ]
펄럭이는 깃발

[ 10.15 7주 2 ]
모션블러를 간단하게 구현하는 법
(이전픽셀과 현재픽셀 사이의 speed vector를 이용해서..)

다음 프레임이 그려질 때, 이전거를 싹 지우지 말아보자
DrawfullScreenRect

* 물결 만들기
	distance(A,B)

병렬프로세싱에서 가장 하지말아야 할 것은 '분기'
	ceil() 올림
	clamp(A,0,1) 0보다 작은건 0 1보다 큰건 1?
	Color 의 - 값은 0 으로 친다









